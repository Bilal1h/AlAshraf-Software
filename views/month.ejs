<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=month%> Month Budget</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background: #1a202c;
            color: #cbd5e0;
        }
        .card {
            background: #2d3748;
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.5);
        }
        .card h2 {
            font-size: 1.5rem;
            font-weight: 700;
            color: #edf2f7;
        }
        .card p {
            font-size: 2rem;
            font-weight: 700;
            margin-top: 1rem;
        }
        .card a {
            margin-top: 2rem;
            display: inline-block;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            font-size: 1rem;
            font-weight: 600;
            transition: background 0.3s ease, transform 0.3s ease;
        }
        .card a:hover {
            transform: translateY(-5px);
        }
        .cardfinal {
            display: flex;
            justify-content: center;
            /* align-items: center; */
            flex-direction:column ;
            background: #fa4646;
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .cardfinal:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.5);
        }
        .cardfinal h2 {
            font-size: 1.5rem;
            font-weight: 700;
            color: #edf2f7;
        }
        .cardfinal p {
            font-size: 2rem;
            font-weight: 700;
            margin-top: 1rem;
            color: #fff;
        }

        .cardfinal a:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body class="flex flex-col items-center justify-center min-h-screen p-6">
    <a href="/profile" class="bg-blue-800 text-white px-2 py-2 mb-6">Go Back</a>
    <!-- Page Title -->
    <h1 class="text-white text-4xl font-bold mb-8 shadow-lg bg-black bg-opacity-30 px-8 py-4 rounded-lg">Monthly Overview - <span id="monthName"><%=month%></span></h1>

    <!-- Cards Section -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl w-full">
        
        <!-- Card 1: Total Budget to be Collected -->
        <div class="card">
            <h2>Amount to be Collected</h2>
            <p class="text-blue-400">Rs. <%=totaltobecollected%></p>
        </div>

        <!-- Card 2: Total Budget Collected -->
        <div class="card">
            <h2>Amount Collected</h2>
            <p class="text-green-400">Rs. <%=totalhousescollected%></p>
        </div>

        <!-- Card 3: Total Houses Budget Collected -->
        <div class="card">
            <h2>Remaining Amount To be Recieved</h2>
            <p class="text-green-400">Rs. <%=finalSumOfRemaining%></p>
        </div>

        <!-- Card 4: Extra Utilities Bill -->
        <div class="card">
            <h2>Expenses / Bills</h2>
            <p class="text-red-400">Rs. <%=totalUtilityAmount%></p>
            <a href="/<%=Year%>/month/<%=month%>/addutility" class="bg-red-500 text-white">Add an Expense / Bill</a>
        </div>

        <!-- Card 5: Unpaid Houses -->
        <div class="card">
            <h2>Unpaid Houses</h2>
            <p class="text-gray-400"><%=lengthOfUnpaidHouses%> Houses</p>
            <a href="/show/<%=Year%>/<%=month%>/unpaidhouses" class="bg-gray-500 text-white">View all Unpaid Houses</a>
        </div>
        
        <!-- Card 6: Unpaid Houses -->
        <div class="cardfinal">
            <h2>Net Total</h2>
            <p class="text-gray-400">Rs. <%=netTotalAmount %></p>
        </div>
    </div>
        
    </div>

    <!-- Action Buttons -->
    <div class="flex gap-6 mt-12">
        <!-- Show Categories Button -->
        <a href="/showcategories/<%=Year%>/<%=month%>" class="bg-yellow-500 text-white py-3 px-8 rounded-lg shadow-lg text-lg font-semibold hover:bg-yellow-600 transition transform hover:scale-105">
            View by Categories
        </a>
        <!-- Show Utilities Button -->
        <a href="/<%=Year%>/month/<%=month%>/showutilities" class="bg-red-500 text-white py-3 px-8 rounded-lg shadow-lg text-lg font-semibold hover:bg-red-600 transition transform hover:scale-105">
            Show Expenses / Bills
        </a>
    </div>

</body>
</html>