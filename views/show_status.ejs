<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>House Status</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-blue-500 via-indigo-500 to-purple-500 text-white min-h-screen flex flex-col items-center justify-center p-6">
  <h1 class="text-4xl font-bold mb-8">House Status for <%= month %></h1>
  <div class="w-[750px] max-w-md bg-white rounded-lg shadow-lg p-8">
    <h2 class="text-2xl font-bold text-gray-800 mb-4">House Number: <%= house.name %></h2>
    <p class="text-lg text-gray-700 mb-2">Category: <%= house.OwnerName %></p>
    <p class="text-lg text-gray-700 mb-2">Category: <%= house.category %></p>
    <p class="text-lg text-gray-700 mb-2">Amount to be Collected: <%= house.amount_to_be_collected %> Rs.</p>
    <p class="text-lg text-gray-700 mb-2">Floors: <%= house.floors %></p>
    <p class="text-lg text-gray-700 mb-2">Amount Collected: <%= house.is_amount_collected ? 'Yes' : 'No' %></p>
  </div>
</body>
</html>